from django.urls import path
# from FilesManager.views import FileUploadView, FileDetailView, FileRemoveView

urlpatterns = [
    # path('upload', FileUploadView.as_view(), name='UploadImage'),
    # path('get-url', FileDetailView.as_view(), name='get-link'),
    # path('remove', FileRemoveView.as_view(), name='delete-file'),
]
